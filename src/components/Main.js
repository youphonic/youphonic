import React from 'react';

import AppContainer from '../containers/AppContainer';

const Main = () => (
  <div id="outer-container">
    <main id="page-wrap">
      <AppContainer />
    </main>
  </div>
);

export default Main;
